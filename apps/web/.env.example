# Server-only upstream API for Next.js rewrites (set on Railway web service)
API_PROXY_TARGET=https://api-production-e60c.up.railway.app/api/v1

# Optional — browser always uses /api/v1 via client.ts; kept for SSR/server-side fetches
NEXT_PUBLIC_API_URL=/api/v1

NEXT_PUBLIC_GOOGLE_CLIENT_ID=
