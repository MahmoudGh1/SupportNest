# SupportNest — API Endpoints Summary

## Tenants
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tenants/:id` | Get tenant details | supervisor |
| PATCH | `/tenants/:id` | Update tenant settings | supervisor |

## Customers
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/customers` | List all customers | supervisor |
| GET | `/customers/:id` | Get single customer | agent, supervisor |
| POST | `/customers` | Create customer | agent, supervisor |
| PATCH | `/customers/:id` | Update customer | agent, supervisor |

## Agents
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/agents` | List all agents | supervisor |
| GET | `/agents/:id` | Get single agent | agent, supervisor |
| POST | `/agents` | Create agent | supervisor |
| PATCH | `/agents/:id` | Update agent role or status | supervisor |

## Tickets
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tickets` | List all tickets with filters | agent, supervisor |
| GET | `/tickets/:id` | Get full ticket details | agent, supervisor |
| POST | `/tickets` | Create new ticket | agent, supervisor |
| PATCH | `/tickets/:id` | Update ticket status or assignment | agent, supervisor |
| GET | `/tickets/:id/messages` | Get all ticket messages | agent, supervisor |
| POST | `/tickets/:id/messages` | Add message to ticket | agent, supervisor |
| GET | `/tickets/:id/escalations` | Get escalation history | agent, supervisor |
| POST | `/tickets/:id/escalate` | Escalate ticket to next tier | agent, supervisor |

## Identity Verifications
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tickets/:id/verifications` | Get verification history | agent, supervisor |
| POST | `/tickets/:id/verifications` | Submit verification attempt | agent, supervisor |

## Tier Actions
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tickets/:id/actions` | Get all actions on a ticket | agent, supervisor |
| POST | `/tickets/:id/actions` | Trigger a tier action | agent, supervisor |

## SLA Logs
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tickets/:id/sla` | Get SLA log for a ticket | agent, supervisor |
| GET | `/sla-logs` | Get all SLA logs with filters | supervisor |

## CSAT Scores
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/tickets/:id/csat` | Get CSAT score for a ticket | agent, supervisor |
| POST | `/tickets/:id/csat` | Submit CSAT score | public |
| GET | `/csat-scores` | Get all CSAT scores with filters | supervisor |

## Reporting
| Method | Endpoint | Description | Role |
|---|---|---|---|
| GET | `/reports/ticket-volume` | Ticket count by status, tier, or channel | supervisor |
| GET | `/reports/resolution-rates` | AI vs human resolution breakdown | supervisor |
| GET | `/reports/sla-compliance` | SLA breach rate per tier | supervisor |
| GET | `/reports/csat-summary` | Average CSAT over time range | supervisor |
| GET | `/reports/agent-workload` | Ticket count per agent | supervisor |

---

**Total: 27 endpoints** across 8 resource groups — all prefixed with `https://api.supportnest.io/v1`
